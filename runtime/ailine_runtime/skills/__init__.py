"""Runtime helpers for loading skill source paths.

Skills are primarily authored under `.claude/skills/` (Claude Code convention).
The runtime uses DeepAgents `skills=` parameter to discover/load them dynamically.
"""
